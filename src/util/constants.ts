/** Project-wide constants. */

export const BOT_NAME = 'Trove';

/** Brand accent color (indigo) used in embeds. */
export const BRAND_COLOR = 0x6366f1;

/** Public repository URL. Replace YOUR-USERNAME before publishing. */
export const REPO_URL = 'https://github.com/YOUR-USERNAME/trove';

/** One-line description shown in /about and /help. */
export const TAGLINE =
  "See a wallet's full on-chain portfolio across many chains — read-only, no keys.";
